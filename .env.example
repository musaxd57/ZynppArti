# ZynppArti ortam değişkenleri — gerçek .env commit'lenmez (bkz. .gitignore)
# Faz 0'da boş; ileriki fazlarda DB/Storage/AI anahtarları buraya eklenecek.

# Copilot AI (Fikir 1) anahtarları apps/web/.env.local içine konur → bkz. apps/web/.env.example
#   AKASHML_API_KEY / OPENAI_API_KEY / ANTHROPIC_API_KEY (zorluk-bazlı router + fallback)

# Örnek (Faz 3+):
# DATABASE_URL=
# S3_ENDPOINT=
